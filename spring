1.springMVC的工作流程

客户端发送url请求，请求经过dispaterServlet核心控制器，并将请求发送到的HandlerMapping映射器，HandlerMapping找到url对应的Controller控制器并返回给dispaterServelet核心控制器
，再发送到HandlerAdapter适配器，然后调用对应的Controller方法，经过一系列处理后，返回数据模型和视图信息给HandlerApater适配器
，然后适配器将信息发回给dispaterServelet核心控制器，核心控制器再将信息发到视图解析器进行解析，视图解析器返回结果给核心控制器，最后返回前端。

2.SpringIoc的初始化过程
用Resource定位配置文件
创建BeanFactory的实现类。
beanDifinationReader加载配置文件，并将配置文件的bean都解析成BeanDifination对象并保存到BeanDifinationRegistry的实现类中，BeanDifinationRegistry的实现类
封装了一个HashMap，以beanName为键，对象为值进行保存。
Spring再对BeanDifination对象进行实例化和赋值形成一个装有对象的容器。

spring扫码
注册bean对象到ioc容器中。


3.springAOP是什么？

AOP就是面向切面编程，是对整个应用的横切关注点进行模块化的编程，让每个业务方法都专注于执行业务方面代码，其他的如日志，异常捕获，事务等具有普遍性的代码
交给切面来执行，切面负责实现横切功能。SpringAop的原理动态代理，Spring对有实现接口的bean进行Jdk动态代理，对没有实现接口的bean进行cglib代理，如果一个类是
final类并没有实现接口将无法实现AOP功能。

4.bean的生命周期？

通过BeanFactory创建bean实例

对bean设置初始值和需要引用的对象

执行bean的初始化方法

ioc容器关闭时执行bean的销毁方法
